{
  "$schema": "../schemas/profile.schema.json",
  "schemaVersion": 3,
  "id": "pointer-grab-x11-probe",
  "name": "BionicX cross-client X11 passive pointer-grab probe",
  "display": {
    "dpi": 144,
    "socket": "filesystem"
  },
  "launch": {
    "executable": "${APP}/bin/pointer-grab-x11-probe",
    "workingDirectory": "${APP}",
    "arguments": [
      "4"
    ],
    "environment": {
      "DISPLAY": "${DISPLAY}",
      "HOME": "${HOME}",
      "TMPDIR": "${TMP}",
      "LANG": "C.UTF-8"
    }
  }
}
