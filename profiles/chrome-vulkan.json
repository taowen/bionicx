{
  "$schema": "../schemas/profile.schema.json",
  "schemaVersion": 2,
  "id": "chrome",
  "name": "Google Chrome stable ARM64 ANGLE Vulkan test",
  "display": {"dpi": 144, "socket": "filesystem"},
  "hostServices": ["vulkan"],
  "launch": {
    "mode": "direct",
    "executable": "${APP}/opt/google/chrome/chrome",
    "workingDirectory": "${APP}/opt/google/chrome",
    "argv0": "${APP}/opt/google/chrome/google-chrome",
    "arguments": [
      "--no-sandbox",
      "--disable-breakpad",
      "--disable-crashpad-for-testing",
      "--disable-dev-shm-usage",
      "--no-first-run",
      "--no-default-browser-check",
      "--window-position=0,0",
      "--window-size=1920,1080",
      "--start-maximized",
      "--user-data-dir=${HOME}/profile-vulkan",
      "--ozone-platform=x11",
      "--ui-toolkit=gtk",
      "--use-gl=angle",
      "--use-angle=vulkan",
      "--disable-features=SkiaGraphite",
      "--enable-logging=stderr",
      "about:blank"
    ],
    "environment": {
      "DISPLAY": "${DISPLAY}",
      "HOME": "${HOME}",
      "TMPDIR": "${TMP}",
      "XDG_RUNTIME_DIR": "${TMP}/runtime",
      "XDG_DATA_DIRS": "${APP}/share:${RUNTIME}/usr/share",
      "CHROME_WRAPPER": "${APP}/opt/google/chrome/google-chrome",
      "CHROME_VERSION_EXTRA": "stable",
      "GNOME_DISABLE_CRASH_DIALOG": "SET_BY_GOOGLE_CHROME",
      "FONTCONFIG_PATH": "${APP}/etc/fonts",
      "FONTCONFIG_FILE": "fonts.conf",
      "GSETTINGS_SCHEMA_DIR": "${RUNTIME}/usr/share/glib-2.0/schemas",
      "GSETTINGS_BACKEND": "memory",
      "GDK_PIXBUF_MODULE_FILE": "${RUNTIME}/usr/lib/aarch64-linux-gnu/gdk-pixbuf-2.0/2.10.0/loaders.cache",
      "LD_LIBRARY_PATH": "${APP}/lib:${APP}/opt/google/chrome:${RUNTIME}/usr/lib:${RUNTIME}/usr/lib/aarch64-linux-gnu",
      "VK_ICD_FILENAMES": "${APP}/share/vulkan/icd.d/vortek_icd.json",
      "VORTEK_SERVER_PATH": "${RUNTIME}/tmp/.vortek/V0",
      "LANG": "C"
    }
  }
}
