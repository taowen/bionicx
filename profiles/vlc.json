{
  "$schema": "../schemas/profile.schema.json",
  "schemaVersion": 1,
  "id": "vlc",
  "name": "VLC (Debian trixie ARM64)",
  "display": {"dpi": 144, "socket": "filesystem"},
  "launch": {
    "mode": "loader",
    "loader": "${RUNTIME}/usr/lib/ld-linux-aarch64.so.1",
    "libraryPath": "${RUNTIME}/usr/lib:${RUNTIME}/usr/lib/aarch64-linux-gnu",
    "executable": "${RUNTIME}/usr/bin/vlc",
    "workingDirectory": "${HOME}",
    "arguments": ["--loop", "--fullscreen", "--no-qt-privacy-ask", "--no-metadata-network-access", "--no-audio", "--no-disable-screensaver", "--vout=xcb_x11", "--no-video-title-show", "--demux=rawvid", "--rawvid-width=320", "--rawvid-height=180", "--rawvid-fps=30", "--rawvid-chroma=I420", "${APP}/fixtures/bionicx-motion.i420"],
    "environment": {
      "DISPLAY": "${DISPLAY}",
      "HOME": "${HOME}",
      "TMPDIR": "${TMP}",
      "XDG_RUNTIME_DIR": "${TMP}/runtime",
      "XDG_CONFIG_HOME": "${HOME}/.config",
      "XDG_CACHE_HOME": "${HOME}/.cache",
      "XDG_DATA_HOME": "${HOME}/.local/share",
      "XDG_DATA_DIRS": "${RUNTIME}/usr/local/share:${RUNTIME}/usr/share",
      "FONTCONFIG_PATH": "${RUNTIME}/etc/fonts",
      "FONTCONFIG_FILE": "fonts.conf",
      "GSETTINGS_SCHEMA_DIR": "${RUNTIME}/usr/share/glib-2.0/schemas",
      "GSETTINGS_BACKEND": "memory",
      "GDK_PIXBUF_MODULE_FILE": "${RUNTIME}/usr/lib/aarch64-linux-gnu/gdk-pixbuf-2.0/2.10.0/loaders.cache",
      "VLC_PLUGIN_PATH": "${RUNTIME}/usr/lib/aarch64-linux-gnu/vlc/plugins",
      "QT_PLUGIN_PATH": "${RUNTIME}/usr/lib/aarch64-linux-gnu/qt5/plugins",
      "QT_QPA_PLATFORM": "xcb",
      "QT_XCB_GL_INTEGRATION": "none",
      "NO_AT_BRIDGE": "1",
      "PATH": "${RUNTIME}/usr/bin:${RUNTIME}/bin:/product/bin:/system/bin",
      "LANG": "C.UTF-8"
    }
  }
}
