{
  "$schema": "../schemas/profile.schema.json",
  "schemaVersion": 3,
  "id": "runtime-probe",
  "name": "BionicX glibc/kernel/X11 runtime probe",
  "display": {
    "dpi": 144,
    "socket": "filesystem"
  },
  "launch": {
    "executable": "${APP}/bin/runtime-probe",
    "workingDirectory": "${APP}",
    "arguments": [
      "--duration",
      "8"
    ],
    "environment": {
      "DISPLAY": "${DISPLAY}",
      "HOME": "${HOME}",
      "TMPDIR": "${TMP}",
      "LANG": "en_US.UTF-8",
      "LOCPATH": "${RUNTIME}/usr/lib/locale"
    }
  }
}
