{
  "$schema": "../schemas/profile.schema.json",
  "schemaVersion": 3,
  "id": "qbittorrent",
  "name": "qBittorrent (Debian trixie ARM64)",
  "display": {
    "dpi": 144,
    "socket": "filesystem"
  },
  "hostServices": [
    "dbus"
  ],
  "launch": {
    "executable": "${RUNTIME}/usr/bin/qbittorrent",
    "workingDirectory": "${HOME}",
    "arguments": [
      "${APP}/fixtures/bionicx-webseed.torrent"
    ],
    "environment": {
      "DISPLAY": "${DISPLAY}",
      "HOME": "${HOME}",
      "TMPDIR": "${CACHE}",
      "XDG_RUNTIME_DIR": "${TMP}/runtime",
      "XDG_CONFIG_HOME": "${HOME}/.config",
      "XDG_CACHE_HOME": "${HOME}/.cache",
      "XDG_DATA_HOME": "${HOME}/.local/share",
      "XDG_DATA_DIRS": "${RUNTIME}/usr/local/share:${RUNTIME}/usr/share",
      "QT_QPA_PLATFORM": "xcb",
      "QT_PLUGIN_PATH": "${RUNTIME}/usr/lib/aarch64-linux-gnu/qt6/plugins",
      "QT_XKB_CONFIG_ROOT": "${RUNTIME}/usr/share/X11/xkb",
      "NO_AT_BRIDGE": "1",
      "PATH": "${RUNTIME}/usr/bin:${RUNTIME}/bin:/product/bin:/system/bin",
      "LANG": "C.UTF-8"
    }
  }
}
