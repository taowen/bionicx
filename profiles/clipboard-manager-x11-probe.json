{
  "$schema": "../schemas/profile.schema.json",
  "schemaVersion": 3,
  "id": "clipboard-manager-x11-probe",
  "name": "BionicX clipboard manager persist probe",
  "display": {
    "dpi": 144,
    "socket": "filesystem"
  },
  "launch": {
    "executable": "${APP}/bin/clipboard-manager-x11-probe",
    "workingDirectory": "${APP}",
    "arguments": [
      "3"
    ],
    "environment": {
      "DISPLAY": "${DISPLAY}",
      "HOME": "${HOME}",
      "TMPDIR": "${TMP}",
      "LANG": "C.UTF-8"
    }
  }
}
