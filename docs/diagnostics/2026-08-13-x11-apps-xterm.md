# First package-installed desktop clients on the clean seed

Device `01408BH601027129` was rebuilt from seed
`ed998c095a1b9384b1f022d06101ac3fc3c61761ac751546bd84edca298e44e2`
plus Debian `hello`. `bxapt install x11-apps xterm` unpacked in one
Pre-Depends wave (`remaining=0`) and configured both packages.

`dpkg --audit` is empty. `x11-apps` is `7.7+11+b2 ii`; `xterm` is `398-1 ii`.

Untraced launches:

- `profiles/xclock.json` shows a live digital clock (screenshots 24s apart).
- `profiles/xterm.json` starts Debian bash 5.2; an injected `pwd` prints
  `/data/user/0/io.taowen.bx/files/homes/xterm`.
