# V2509A: five package-installed GUIs

Device `10AFA31610002QH` (vivo V2509A, Android 16, Mali, 4 KiB, UID
`u0_a381`, no `su`). Shared seed
`ed998c095a1b9384b1f022d06101ac3fc3c61761ac751546bd84edca298e44e2`.

f2fs app-data denies `link(2)`. dpkg backs up its status file with
`link(status, status-old)` and failed with Permission denied. The runtime
now copies the regular file when `link`/`linkat` return EACCES/EPERM.
Host probe: `BIONICX_FORCE_LINK_COPY=1` in `runtime-contract-probe`.
`bxapt set packages/trixie-popular.txt` then left xterm, evince, keepassxc,
krita and firefox-esr `ii`.

`examples/vivo-apps/run.sh` launched each profile untraced. First frames
were a gray Android surface; waiting ~10s produced guest pixels:

- `evidence/vivo-10AFA31610002QH/xterm.png` — `bash-5.2$`
- `evidence/vivo-10AFA31610002QH/evince.png` — fixture PDF page 1
- `evidence/vivo-10AFA31610002QH/keepassxc.png` — KeePassXC 2.7.10 welcome
- `evidence/vivo-10AFA31610002QH/firefox-esr.png` — https://example.com/
- Krita starts untraced then `exited with 139` (SIGSEGV) on this Mali. Not
  accepted. Pocket FIT Adreno still has the painted workflow.
