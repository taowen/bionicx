# Integration test matrix

## Workflow

Each gap follows the same loop:

1. reproduce it with the smallest genuine glibc client possible;
2. retain raw symptoms and the decisive trace in `docs/diagnostics/`;
3. add a machine-readable `BXTEST` regression;
4. implement the smallest runtime or X server correction;
5. verify on Android, commit that single capability, and push it;
6. rerun all lower layers before retrying Chrome or WPS.

`PASS` means a request completed with the expected observable result, not just
that the client did not crash. Interactive checks record screenshots and input
counters. App profiles always run with diagnostics disabled for final proof.

## Layers

| Layer | Controlled client | Coverage | State |
|---|---|---|---|
| ELF/bootstrap and session | `hello-x11`, `direct-argv0-probe`, `session-x11-probe` | glibc loader and explicit `--argv0`, dependency closure, X connection; subreaper adoption, double-fork + `setsid` detached GUI lifetime, normal drain and TERM cleanup | hello passes; direct argv0 plus runtime 21/21; detached session 3/3, adopted child drain and unrooted cleanup pass on x300 |
| X11 core | `x11-probe`, `x11-window-tree`, `clipboard-x11-probe`, `keyboard-grab-x11-probe`, `pointer-grab-x11-probe`, `server-grab-x11-probe`, `save-set-x11-probe`, `map-request-x11-probe`, `reparent-x11-probe`, `get-property-x11-probe`, `grab-x11-probe`, `image-text8-shape-probe`, `send-event-x11-probe` | windows including resolved `CopyFromParent` class and legal `InputOnly` geometry, direct-child `MapSubwindows`, delayed viewability exposure and initial visibility notification, `InternAtom(only_if_exists)` returning `None`, honest extension advertisement (incomplete DRI3 hidden), cross-process recursive window observation, property round trips and enumeration, core fixed-font query/list/text extents, TrueColor allocate/query and cursor recolor, exact two-level keyboard mapping, initial and dynamic `PointerRoot` focus plus `SetInputFocus` target/revert separation, drawing including GC-clipped `CopyArea`, PolyPoint origin/previous coordinates and tiled `CWBackPixmap` clear plus Map+MoveResize background paints, cross-client UTF8 selection transfer and cleanup, active/passive keyboard and pointer grabs with contention/ungrab/owner-events/disconnect/automatic-release routing, cursor override, `confineTo` clamping, synchronous freeze and `ReplayPointer` click-through, exact pointer event state, server-grab request/setup deferral and disconnect cleanup, WM save-set rescue/reparent/map/coordinate preservation, events, modifier ordering; two-connection `SubstructureRedirect`/`MapRequest` for normal and `TYPE_DOCK` windows plus grab-owner map; `ReparentWindow` auto-unmap (`from-configure`) and remap into a manager frame, including under `GrabServer`; `SendEvent` to `PointerWindow`/`InputFocus` including `None` discard, `PointerRoot` focus, pointer-inside-focus delivery, event-mask routing, propagate, and `GrabServer` | core 33/33 plus ImageText8 background/glyph readback and SHAPE 1.1 Rectangles/extents 6/6, Chrome window tree with zero X errors, clipboard 5/5, keyboard grab 9/9, pointer grab 6/6, server grab 5/5, save set 2/2; MapRequest and ReparentWindow protocol probes replace IceWM/xfwm4 wrappers; GrabServer owner ConfigureWindow 7/7; framed dock XTEST click and ReplayPointer 7/7; SendEvent PointerWindow/InputFocus 8/8; virtual core-font glyph shapes are approximate, dynamic partial/full visibility changes and other `AllowEvents` modes are pending |
| X11 desktop | `xrender-x11-probe`, `xkb-x11-probe`, `font-xft-probe`, `gtk3-probe`, `gtk-template-probe`, `gdk-pixbuf-svg-probe`, `gtk-icon-theme-probe`, `gtk-desktop-icon-probe`, `gtk-menu-probe`, `xi-x11-probe`, `input-settings-x11-probe`, `randr-x11-probe`, `xfixes-x11-probe`, `xsettings-x11-probe`, `xsettings-typed-x11-probe`, `clipboard-manager-x11-probe`, `damage-x11-probe`, `composite-x11-probe`, `compositor-x11-probe`, `named-pixmap-resize-x11-probe`, `resize-screen-x11-probe`, `mit-shm-present-x11-probe`, `sync-x11-probe`, `xres-x11-probe` | Render ARGB32/A8 pictures, creation-time repeat, rectangle and 1-bit pixmap clips with clip origins, filters, solid/gradient sources, Clear/Src/Over/In/OutReverse/Add/Saturate pixel semantics including unused-alpha dest Over a window Picture that tracks ConfigureWindow, and CompositeTrapezoids, glyph-set upload/lifecycle and CompositeGlyphs8 alpha-over; XFixes mask-7 selection owner/set/window-destroy/client-close lifecycle, regions and ShapeInput, 4.0 cursor image/name/change/hide/show, ChangeSaveSet, region combine, Set/Translate/Invert/Extents/CreateRegionFromWindow, SetGCClipRegion/SetPictureClipRegion, and overlay Bounding/Clip shapes; RandR 1.5 including Set/GetCrtcTransform, ListOutputProperties and GTK GetOutputInfo/EDID; Super/Mod4; SYNC 3.1 counters, Await, and fences; X-Resource 1.0 pixmap accounting; XInput2 master ButtonClass, XIQueryPointer, event masks, pointer/keyboard XIGrabDevice and live DeviceEvents with normal Enter/Leave crossing and button-state masks, XI1 device properties/button-map/feedback/mode, XIWarpPointer and XISet/GetFocus; XKB printable map/event selection/device metadata/names, real xkbcommon compilation and live StateNotify, named Caps/Num/Scroll indicators and GetIndicatorState; two-connection XSETTINGS manager register (`_XSETTINGS_S0`, `_XSETTINGS_SETTINGS`, `MANAGER`, `RESOURCE_MANAGER`) including under `GrabServer`; typed XSETTINGS 6/6; typed `Xft/DPI`/`Gtk/FontName` payload and peer `PropertyNotify` on the manager window; `CLIPBOARD_MANAGER` persist-after-owner-destroy 6/6; DAMAGE 1.1 QueryVersion/Create/Add/Subtract/Destroy with Add- and PutImage-triggered DamageNotify; Composite 0.3 RedirectWindow/RedirectSubwindows/NameWindowPixmap/GetImage/GetOverlayWindow/ReleaseOverlayWindow; xfwm4-shaped `RedirectSubwindows(root)` compositor probe with unmapped overlay until client Map and GLX ClientInfo/SetClientInfoARB/2ARB; overlay child output window; A8/repeat/clip `Composite` paint burst after `UngrabServer`; `CreatePicture` present into the overlay child; overlay output tree stays on-screen; `NameWindowPixmap` frozen after resize until re-named; MIT-SHM CreatePixmap plus PresentPixmap software present | desktop 9/9 with zero X errors, successful master pointer/keyboard grabs, exact XI2 crossing/device wire validation, exact Saturate, pixmap-clip, unused-alpha title-Over, window-picture-resize, and trapezoid pixels, Fontconfig/Xft 4/4, real GTK3/Pango/label lifecycle 10/10, and five deterministic complete file-chooser paints pass; gtk-template latin1 `g_convert` 7/7; gdk-pixbuf SVG loader 4/4; GtkIconTheme 7/7; desktop icons Adwaita 6/6; GTK menu popup 13/13 including compositor, tall/early/Garcon interior paint, button-press-event; typed XSETTINGS 6/6; clipboard-manager persist 6/6; input-settings 10/10 including Super/Mod4; randr 9/9 including 1.5 and Set/GetCrtcTransform; xfixes 11/11 including region-combine, region-transform, region-clip, and ShapeInput; SYNC 9/9; XRes 5/5; DAMAGE 1.1 family 7/7 including PutImage notify and GrabServer; window content notifies flush once per request burst so GTK expose and xfwm4 repair are not presented per glyph; Composite 0.3 family 8/8 including overlay Get/Release and GrabServer; xfwm4-shaped root compositor 16/16 (overlay unmapped at Get, stays top after LowerWindow, overlay-shape-regions, GLX client-info, overlay child output, A8 paint burst after UngrabServer, present Composite to overlay child, full-screen output buffer with freed 1x1 picture and peer `_XROOTPMAP_ID` tile, depth-24 pixmap CreatePicture, 24-in-32 visual-format Composite stays opaque); NameWindowPixmap resize 7/7; root-redirect resize screen 5/5 including Android screenshot; MIT-SHM CreatePixmap+Present 8/8 |
| Host GPU | `glx-probe`, `vulkan-wsi` / `vulkan-present` / `vulkan-lifetime` | Real glibc clients through pinned BionicX Gladio and Vortek submodules; complete GLX/GLES capability, shader, cache, draw, readback, and compositor coverage; optional app-private Vulkan host service, glibc Vulkan loader/ICD discovery, shared-ring RPC into a Bionic process, native vendor physical-device/queue/memory discovery, honest device-extension filtering, Xlib/XCB WSI translation, logical-device/swapchain commands, coherent mapped vertex/index/uniform/staging upload, buffer-to-image copies and barriers, sampled-image descriptors, SPIR-V graphics pipelines/render passes/indexed draws, Vulkan 1.3 vertex binding with exact optional-array NULL fidelity, present-semaphore ownership, and imported `AHardwareBuffer` presentation | GLX client 5/5 plus final Android screenshot pixel assertion pass; Chrome's normal untraced profile displays its complete UI through ANGLE/Ganesh OpenGL; Vulkan probes are 5+3+1 (wsi/present/lifetime) and bind both Xlib and XCB surfaces to a real 640x360 X window, verifies unimplemented fragment shading rate is hidden, requires the X11 BGRA visual and advertised `VK_FORMAT_B8G8R8A8_UNORM`, uploads mapped buffers, stages and samples a texture, binds uniform and combined-image descriptors, executes an indexed `vkCmdBindVertexBuffers2` draw with NULL sizes/strides, consumes the present semaphore without a pre-present idle, rotates/resizes/recreates the swapchain, and passes exact two-color Android screenshot assertions through `/vendor/lib64/hw/vulkan.mali.so`; Chrome ANGLE Vulkan creates 68 observed graphics pipelines successfully, while multi-image swapchain lifecycle remains under investigation |
| libc/kernel | `runtime-probe` | threads, robust owner death, epoll, signals, processes, IPC, sockets, mmap | 20/20 passing untraced on x300 with BionicX Android-compatible glibc 2.41 |
| glibc/Bionic bridge | `runtime-probe`, `command-runtime-probe`, `network-x11-probe` | mandatory cross-process System V semaphore namespace, process/IPC primitives, child execution, Android active-network DNS injection, live DNS/TCP/HTTP and X11 rendering | controlled runtime and network probes pass untraced under the ordinary app UID |
| terminal | Debian xterm 398 and bash 5.2 | apt/dpkg package ownership, Fontconfig/core-font fallback, PTY, inherited glibc child execution, core keyboard mapping, Android-injected command input and rendered output | real package-installed xterm starts Debian bash, executes `echo bionicx-xterm-ok` and `pwd`, and remains interactive untraced under the ordinary app UID |
| Debian GUI applications | Thunar 4.20.2, Mousepad 0.6.3, Ristretto 0.13.3 | shared apt/dpkg dependency image; GTK menus/dialog focus, pointer and keyboard grabs, file creation, text edit/save/cold reopen, PNG decode/thumbnail/image selection | all three package-installed ARM64 applications pass real workflows under ordinary app UID: Thunar creates and displays a 0700 directory, Mousepad saves and cold-reopens a 26-byte edited document, and Ristretto decodes two PNGs and switches images; no per-app shared-library copies |
| XFCE session | `xfwm4` 4.20.0, `xfce4-panel` 4.20.4, `xfdesktop` 4.20.1, `xfsettingsd` plus Thunar/Mousepad | package-installed WM/panel/desktop/settings without `xfce4-session`; compositor on (`--vblank=off`); honest dock MapRequest/Reparent and `GetProperty(G_MAXLONG)`; focus/resize/close/reopen; XRes/XSync/Super/RandR 1.5; FHS `dlopen@GLIBC_2.34` for gdk-pixbuf SVG; Thunar owns `org.xfce.Thunar` before xfdesktop; session xfconf pins `Net/ThemeName`/`Net/IconThemeName=Adwaita` and panel `dark-mode=false` because seed Tango is a Geany stub and Debian's panel defaults to dark menus; `XDG_MENU_PREFIX=xfce-` so garcon loads `xfce-applications.menu` | vivo Mali `xfce-session-accept` 12/12 including `_NET_WM_CM_S0` and `_XSETTINGS_S0` after resetting Thunar's saved maximized geometry; panel/Thunar stay up after Adwaita SVG load; no second Thunar racing the bus name; desktop Home/File System and Applications menu use Adwaita/hicolor icons; `xfce4-popup-applicationsmenu` maps a 185x324 Adwaita menu whose pixmap is ~83% light after Render 0.4 advertises CompositeTrapezoids; XTEST/adb tap on the Applications button still does not open it; a 116x53 panel tooltip can stay black; no per-app `libc.so.6` |
| creative and media applications | GIMP 3.0.4, Inkscape 1.4, VLC 3.0.23 | packaged plug-in child processes, GEGL/Babl image editing, SVG/Cairo rendering, Qt/XCB video, AVI demux, raw I420 and PCM decode, PulseAudio-to-AAudio output | all three package-installed ARM64 applications pass under the ordinary app UID: GIMP decodes a PNG and accepts an Android-injected painted stroke, Inkscape renders a complex SVG, and VLC simultaneously decodes a looping 30 fps I420 stream and 48 kHz stereo PCM into XPutImage and Android AAudio; AudioFlinger reports one active track with zero write errors and underruns; no per-app library copies |
| development, transfer and mail applications | Geany 2.0, FileZilla 3.68.1, Thunderbird 140.13 ESR | one shared apt/dpkg rootfs; compiled-in FHS data paths, GTK and wxWidgets, XFixes cursor metadata, XI2 client pointer, Mozilla direct launch and child processes | all three package-installed ARM64 applications run untraced as the ordinary app UID: Geany highlights, edits and durably saves C source; FileZilla renders its complete main window and interactive Site Manager; Thunderbird renders account setup, accepts form input and keeps four Mozilla processes alive; no per-app library copies |
| desktop services | `font-xft-probe`, `audio-probe`, `dbus-probe`, `bionicx-open`, WPS workflow | app-private Fontconfig discovery, separate regular/bold FreeType faces and real Xft rendering; deterministic Calibri/Cambria/Arial/Times aliases to static Liberation fonts; compiled app-private GTK GSettings schemas with memory backend; glibc URI dispatcher and PDF handler; profile-selected PulseAudio protocol bridge into an Android AAudio sink; profile-selected unmodified Debian session `dbus-daemon`, private Unix socket, automatic client address and stale-socket restart; locale, broader MIME classes and optional D-Bus-activated services | controlled Fontconfig/Xft 4/4 and WPS Latin regular/bold cold reopen pass; Chrome has zero schema/dconf errors; Writer export opens in detached WPS PDF through the unrooted app-private dispatcher; the audio probe writes and drains 240,000 48 kHz stereo frames and AudioFlinger confirms an active zero-error track; `dbus-send ListNames` passes across two force-stop/restart cycles, FileZilla holds two bus connections and Thunderbird owns its Mozilla application name; remaining optional services pending |
| Chromium | Google Chrome stable 151.0.7922.108 ARM64 | pinned/hash-locked package acquisition, apt/dpkg-installed shared Debian 13 trixie rootfs with WPS, IceWM, xterm, XFCE and popular application cohorts, preserved package database/data/plugins and a `GLIBC_2.41` symbol-floor gate, direct patched-PT_INTERP bootstrap, ICU `/proc/self/exe` path, XKB partial-map parsing, Android seccomp feature probes, child Crashpad-policy propagation, Fontconfig, Android DNS bridge, deterministic 1920x1080 X11 window, ANGLE/Ganesh OpenGL on host GLES; separate ANGLE Vulkan profile through Vortek; sandbox and services | the shared system layer checks 2,474 ELF objects and is transferred once by content ID; IceWM remains 4/4 and the normal Chrome profile reaches its complete 1920x1080 UI from that rootfs; it also loads Example Domain through DNS/TCP/TLS and renders a complete hardware-accelerated full-screen UI untraced under the ordinary app UID; ANGLE Vulkan creates its device, acquires the Queue2 graphics queue, creates an AHardwareBuffer swapchain, remains alive, renders the complete browser and internal GPU page without black regions, accepts Android-injected navigation, and reports hardware-accelerated Canvas/compositing/raster/WebGL/WebGPU on the Vortek Adreno Vulkan device; `--no-sandbox` is intentionally retained and remaining services are pending |
| Firefox ESR | Debian trixie ARM64 | shared snapshot rootfs, direct `/proc/self/exe` launch, GTK3/X11 and deterministic local HTML fixture | full local page rendered as the ordinary app UID; device networking was unavailable, so online navigation remains pending |
| Evince | Debian trixie ARM64 | shared snapshot rootfs, Poppler, real MIT-SHM fd import, offset-aware PutImage and GetImage | both pages of the controlled PDF render correctly |
| LibreOffice Writer | Debian trixie ARM64 | shared snapshot rootfs, relocated UNO rc URLs, Android-private `/tmp`, XRender radial gradients, pointer-grab mutation and XKB indicator state | controlled ODT opens, accepts injected editing, saves, and the saved text is verified in `content.xml` |
| office | real WPS 11.1.0.11720 ARM64 | apt/dpkg-installed shared trixie dependencies; Writer, Sheets, Presentation, PDF import/export, clipboard | from the shared rootfs Writer reaches its home screen, creates a real document and accepts editor input untraced; earlier Writer durable copy/paste/cold reopen, structurally verified PDF export, and post-export Open File all pass; Spreadsheets creates/calculates/saves/cold-reopens valid OOXML; Presentation edits, saves, structurally verifies, presents full-screen, exits via Escape, and cold-reopens valid PPTX untraced; WPS PDF opens a structurally checked two-page fixture, renders exact content, navigates, zooms, and cold-reopens untraced |

## Upstream acceptance baseline

Baseline inspected on 2026-08-11 at Pi-Apps commit
`e84fe19d038c310502cbd2e296c549d95729ee4f`.

- Chrome `install-64` installs Google's ARM64 stable/beta/unstable/canary Debian
  packages from `https://dl.google.com/linux/chrome-stable/deb/`. BionicX will
  begin with stable and record its exact package version and hash.
- WPS `install-64` selects `wps-office_11.1.0.11720_arm64.deb`, adds legacy
  WebP/TIFF dependencies for PDF export, Microsoft-compatible fonts, X11
  utilities and `wmctrl`. Pi-Apps removes `wpscloudsvr` and runs WPS offline;
  BionicX preserves that privacy-oriented acceptance baseline.

The upstream scripts are acquisition specifications, not scripts to execute
inside Android. Application payloads and proprietary packages are never
committed to this repository.
