# Vivo V2509A application launch

Runs the five GUIs that prove BionicX on `10AFA31610002QH` after
`bxapt set packages/trixie-popular.txt`. Each launch must be untraced and
leave a screenshot.

```sh
ANDROID_SERIAL=10AFA31610002QH examples/vivo-apps/run.sh
```
